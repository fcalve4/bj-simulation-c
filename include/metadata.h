#ifndef METADATA_H
#define METADATA_H

// Metadata struct for storing user input / game constants

typedef struct {
    int num_decks;
    int h17;
    int ls;
    int enhc;
    int bj_pay;
    int pen;

    int num_simulations;
    int bankroll;
    int wager;
} Metadata;


#endif
